(module experiments.heap-inspector.live-function-transform-test)

(import "coil.primitive" :as primitive)
(import "experiments.heap-inspector.live-reader" :as reader)

(defn coil_live_enter [] (-> i64) 0)
(defn coil_live_exit [] (-> i64) 0)

(reader/lr-dynamic-function-self-check Particle radius)

(defn main [] (-> i64)
  (if (= (radius (Particle :x 42)) 42) 0 1))

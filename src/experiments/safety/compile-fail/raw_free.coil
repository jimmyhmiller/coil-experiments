(module experiments.safety.compile-fail.raw-free)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn main [] (-> i64)
  (let [p (primitive/alloc-heap i64)]
    (primitive/free p)))

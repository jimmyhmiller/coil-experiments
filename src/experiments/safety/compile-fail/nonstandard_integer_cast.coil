(module experiments.safety.compile-fail.nonstandard-integer-cast)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn main [] (-> i64)
  (primitive/cast i64 (primitive/cast u5 7)))

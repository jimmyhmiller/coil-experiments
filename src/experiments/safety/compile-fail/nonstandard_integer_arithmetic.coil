(module experiments.safety.compile-fail.nonstandard-integer-arithmetic)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)
(import "experiments.safety.unsafe" :use [unsafe])

(defn main [] (-> u5)
  (+ (unsafe (primitive/cast u5 7)) (unsafe (primitive/cast u5 1))))

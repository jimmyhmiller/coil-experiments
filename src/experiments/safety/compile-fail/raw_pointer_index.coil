(module experiments.safety.compile-fail.raw-pointer-index)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn main [(p (ptr i64))] (-> i64)
  (primitive/cast i64 (primitive/index p 1)))

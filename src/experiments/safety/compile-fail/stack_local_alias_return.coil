(module experiments.safety.compile-fail.stack-local-alias-return)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn bad [] (-> (ptr i64))
  (let [address (primitive/alloc-stack i64)]
    address))

(defn main [] (-> i64)
  (load (bad)))

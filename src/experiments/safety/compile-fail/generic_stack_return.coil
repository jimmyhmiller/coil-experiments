(module experiments.safety.compile-fail.generic-stack-return)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn bad [T] [] (-> (ptr T))
  (primitive/alloc-stack T))

(defn main [] (-> i64)
  (load (bad [i64])))

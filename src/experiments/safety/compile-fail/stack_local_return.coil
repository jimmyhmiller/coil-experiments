(module experiments.safety.compile-fail.stack-local-return)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn bad [] (-> (ptr i64))
  (primitive/alloc-stack i64))

(defn main [] (-> i64)
  (load (bad)))

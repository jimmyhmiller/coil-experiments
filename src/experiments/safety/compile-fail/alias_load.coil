(module experiments.safety.compile-fail.alias-load)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn main [] (-> i64)
  (let [p (primitive/alloc-stack i64)]
    (primitive/alias-load i64 p)))

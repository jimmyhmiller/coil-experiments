(module experiments.safety.tests.ffi-overlap-trap)
(import "coil.primitive" :as primitive)
(import "experiments.safety.ffi" :use *)

(defn main [] (-> i64)
  (let [data (primitive/alloc-stack (array i8 16))]
    (checked-nonoverlap (primitive/index data 0) 8 (primitive/index data 4) 8)))

(module experiments.safety.tests.null-trap)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn main [] (-> i64)
  (let [address 0]
    (load (primitive/cast (ptr i64) address))))

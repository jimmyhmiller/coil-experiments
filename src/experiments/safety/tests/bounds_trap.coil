(module experiments.safety.tests.bounds-trap)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn main [] (-> i64)
  (let [xs (primitive/alloc-stack (array i64 4))
        index 4]
    (load (primitive/index xs index))))

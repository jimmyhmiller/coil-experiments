(module experiments.safety.tests.signed-cast-trap)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn main [] (-> i64)
  (primitive/cast i64 (primitive/cast i8 128)))

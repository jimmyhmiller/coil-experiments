(module experiments.safety.tests.float-cast-trap)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn main [] (-> i64)
  (primitive/cast i64 (primitive/cast u8 256.0)))

(module experiments.safety.tests.negative-unsigned-cast-trap)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn main [] (-> i64)
  (let [value -1]
    (primitive/cast i64 (primitive/cast u8 value))))

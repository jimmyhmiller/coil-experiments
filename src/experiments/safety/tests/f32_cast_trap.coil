(module experiments.safety.tests.f32-cast-trap)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn main [] (-> i64)
  (primitive/cast i64 (primitive/cast f32 1.0e30)))

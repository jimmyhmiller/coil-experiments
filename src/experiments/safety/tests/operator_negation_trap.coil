(module experiments.safety.tests.operator-negation-trap)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn main [(argc i32) (argv (ptr (ptr i8)))] (-> i64)
  (let [minimum (+ -9223372036854775808 (primitive/cast i64 (- argc argc)))]
    (- 0 minimum)))

(module experiments.safety.tests.ffi-null-buffer-trap)
(import "coil.primitive" :as primitive)
(import "experiments.safety.ffi" :use *)

(defn main [] (-> i64)
  (primitive/cast i64 (checked-buffer (primitive/cast (ptr i64) 0) 1)))

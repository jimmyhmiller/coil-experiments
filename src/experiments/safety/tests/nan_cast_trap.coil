(module experiments.safety.tests.nan-cast-trap)
(import "coil.primitive" :as primitive)
(import "experiments.safety.safety" :use *)

(defn main [] (-> i64)
  (let [nan (primitive/fdiv 0.0 0.0)]
    (primitive/cast i64 nan)))

(module experiments.safety.sums-test)
(import "coil.assert" :use *)
(import "experiments.safety.sums" :use *)

(deftest valid-unwraps
         (assert-eq (expect-some (Some 42)) 42)
         (assert-eq (expect-ok (Ok [i64 i64] 42)) 42)
         (assert-eq (expect-err (Err [i64 i64] 7)) 7))

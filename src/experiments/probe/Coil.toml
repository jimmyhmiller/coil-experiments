[package]
name = "probe"
entry = "demo/main.coil"
source-roots = ["."]

# A `.probe` file is read by the probe reader and becomes a dialect: switch one on
# with `coil run --use <its module name>`.
[readers]
".probe" = "experiments.probe.lang"

[metaprograms]
use = ["experiments.probe.lang"]

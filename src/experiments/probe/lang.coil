(module experiments.probe.lang)

; The `.probe` language: name this module under `[readers]` and `[metaprograms]`
; in Coil.toml and every `.probe` file becomes a module you can `--use`.
(reader-provider "experiments.probe.reader" read-probe)

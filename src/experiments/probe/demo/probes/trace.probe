# coil-module: experiments.probe.demo.trace
# `enter` fires before the call, so it can only read arguments.
enter db/query { print("-> {caller} runs: {sql}"); }
call  db/query { print("<- {result} rows in {elapsed}"); }

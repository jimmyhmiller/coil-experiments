# coil-module: experiments.probe.demo.files-points
# A point library: the DSL's version of a DTrace provider. `use files-points;`
point file-open = call coil.os/open {
  path = arg0;
}

point file-read = call coil.fs/read-file {
  bytes = result.ok.len;
}

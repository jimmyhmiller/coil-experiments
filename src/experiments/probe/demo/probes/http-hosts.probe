# coil-module: experiments.probe.demo.http-hosts
# 5c. Time by host, using an ordinary Coil function from inside the probe.
use http;
import experiments.probe.demo.urls;

http { time by { (url-host url) }; }

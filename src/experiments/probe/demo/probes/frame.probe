# coil-module: experiments.probe.demo.frame
# 3. Profile a span. Reports every 500ms while the program is still running.
report every 500ms;

span frame = call render-frame;

frame               { time; }
call * during frame { time by fn; }

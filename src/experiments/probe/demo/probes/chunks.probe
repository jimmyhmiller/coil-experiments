# coil-module: experiments.probe.demo.chunks
# A callback the program never calls directly: the HTTP client invokes it through
# a function pointer. The probe still sees every call.
call net/count-chunk { count; sum n; print("chunk of {n} bytes via {caller}"); }

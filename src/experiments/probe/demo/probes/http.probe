# coil-module: experiments.probe.demo.http
# A point library for HTTP: name the calls once, give their data friendly names.
point http = call coil.http.client/request | coil.http.client/request-stream {
  method = req.method;
  url    = req.url;
  status = result.ok.status;
}

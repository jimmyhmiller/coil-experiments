# coil-module: experiments.probe.demo.live
# Results do not wait for the program to finish: this snapshot file is rewritten
# every 250ms while the program runs. Try `watch cat frames.txt` beside it.
report every 250ms to "frames.txt";

span frame = call render-frame;
frame { time; }
call * during frame { time by fn; }

[package]
name = "image"

# Image-enabled applications embed Coil's in-process JIT (phase C onward) and
# export native callbacks that its loader resolves from the host executable.
[link]
flags = ["-Wl,-export_dynamic"]

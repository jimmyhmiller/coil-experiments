# Checked arithmetic module

Implement a Coil module with an exported function named `sum-to`. It accepts an
`i64` named `n` and returns the sum of every integer from `0` through `n`, also
as an `i64`. For negative inputs it must return `0`. Include a `main` function
that returns `0`.

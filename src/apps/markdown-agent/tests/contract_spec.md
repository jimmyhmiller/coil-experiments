# Generated smoke contract

Create module `experiments.markdown-agent.markdown-contract` with a `main`
function returning `i64`. It must return zero when the generated-code smoke
contract passes. This contract has no contract of its own.

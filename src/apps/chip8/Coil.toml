[package]
name  = "chip8"
entry = "main.coil"

[build]
# The package name is the namespace segment (experiments.chip8.*); the artifact
# keeps the name it always had.
out = "chip8-gui"

[link]
frameworks = ["AppKit", "CoreGraphics"]
libs       = ["objc"]

# This package holds no Coil modules. cocoa.c is C, built by the native C
# frontend together with the pinned Doom Generic sources -- see
# scripts/doom-play.py. The manifest exists because every workspace member
# needs one.
[package]
name = "doom"

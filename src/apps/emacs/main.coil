(module experiments.emacs.main)
(import "experiments.emacs.core" :as emacs)

(defn main [(argc i32) (argv (ptr (ptr i8)))] (-> i64)
  (do
    (emacs/initialize-c-library)
    (cast i64 (emacs/c-main argc argv))))

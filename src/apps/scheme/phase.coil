(module coil.scheme.phase)

;; Runtime bindings available to natively compiled Scheme transformer
;; procedures (staged metacompilation). This deliberately does not import the
;; reader, source dialect, lint, or lowering metaprograms: phase source has
;; already passed through those transformations before its isolated program is
;; compiled. It DOES include coil.scheme.syntax, whose mixed Code/value
;; signatures only compile inside a metaprogram — which is exactly where phase
;; programs live.
(import "coil.primitive" :use [fnptr-of] :reexport)
(import "coil.scheme.core" :use * :reexport)
(import "coil.scheme.closure" :use * :reexport)
;; The runtime half of the lambda module: lifted transformer bodies call
;; closure-fnptrN/make-closure support that lives beside the lifting pass.
(import "coil.scheme.lambda" :use * :reexport)
(import "coil.scheme.derived" :use * :reexport)
(import "coil.scheme.derived2" :use * :reexport)
(import "coil.scheme.numeric" :use * :reexport)
(import "coil.scheme.stdproc" :use * :reexport)
(import "coil.scheme.port" :use * :reexport)
(import "coil.scheme.value" :use * :reexport)
(import "coil.scheme.heap" :use * :reexport)
(import "coil.scheme.symbol" :use * :reexport)
(import "coil.scheme.syntax" :use * :reexport)
(import "coil.scheme.forms" :use * :reexport)

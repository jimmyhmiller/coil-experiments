[package]
name     = "scheme"
# Importing this module ASCII-folds the importing file's identifiers: Scheme is
# case-insensitive. The compiler learns that from here rather than from a
# namespace spelling compiled into it.
casefold = "lang"

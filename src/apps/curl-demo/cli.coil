(module experiments.curl-demo.cli)
(import "experiments.curl-demo.curl-cli" :as curl-cli)

(defn main [(argc i32) (argv (ptr (ptr i8)))] (-> i64)
  (do
    (curl-cli/initialize-c-library)
    (cast i64 (curl-cli/c-main argc argv))))

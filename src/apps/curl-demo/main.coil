(module experiments.curl-demo.main)
(import "coil.primitive" :use *)
(import "experiments.curl-demo.curl" :as curl)

(extern puts :cc c [(ptr i8)] (-> i32))

(defn main [] (-> i32)
  (do
    (curl/initialize-c-library)
    (puts (cast (ptr i8) (curl/curl_version)))
    0))

(module dependent-negative-nontermination)
(import "experiments.dependent.lang" :use [dependent])

(dependent
  (claim loop (Pi n Nat Nat))
  (define loop (fn n Nat (app loop n)))
  (emit-nat impossible-result (app loop 0)))

(defn main [] (-> i64) 0)


(module dependent-negative-universe-escape)
(import "experiments.dependent.lang" :use [dependent])

(dependent
  ;; U 2 inhabits U 3; cumulativity never allows a larger universe to flow down.
  (check (U 2) (U 1)))

(defn main [] (-> i64) 0)


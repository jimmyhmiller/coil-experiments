(module dependent-negative-duplicate-declaration)
(import "experiments.dependent.lang" :use [dependent])

(dependent
  (data First [(Same [])])
  (data Second [(Same [])]))

(defn main [] (-> i64) 0)


(module wasm-imports-unlinkable.main)

(import "test" :as test)
(import "imports.candidate" :as candidate)

(defn main [] (-> i64)
  (test/__coil_wasm_instantiate)
  (candidate/__coil_wasm_instantiate)
  0)

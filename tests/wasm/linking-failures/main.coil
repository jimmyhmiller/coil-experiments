(module wasm-linking-failures.main)

(import "reexport_f" :as reexport-f)
(import "Mg" :as mg)
(import "Mt" :as mt)
(import "Mm" :as mm)
(import "Ms" :as ms)
(import "linking.candidate" :as candidate)

(defn main [] (-> i64)
  (reexport-f/__coil_wasm_instantiate)
  (mg/__coil_wasm_instantiate)
  (mt/__coil_wasm_instantiate)
  (mm/__coil_wasm_instantiate)
  (ms/__coil_wasm_instantiate)
  (candidate/__coil_wasm_instantiate)
  0)

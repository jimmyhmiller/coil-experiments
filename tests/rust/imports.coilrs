module tests::rust::imports;

use coil::io::{stdout, print_str};
use coil::alloc as alloc;
use coil::str::* except { sb-new };
pub use coil::slice::{slice-len as rust_like_slice_len};

fn main() -> i64 {
    let a = alloc::malloc-allocator();
    if rust_like_slice_len("imports") == 7 { 0 } else { 1 }
}

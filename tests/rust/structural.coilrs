module tests::rust::structural;

item form!(
    atom!("defn"),
    atom!("structural-value"),
    [],
    form!(atom!("->"), atom!("i64")),
    atom!("0")
);

fn main() -> i64 {
    form!(atom!("structural-value"))
}

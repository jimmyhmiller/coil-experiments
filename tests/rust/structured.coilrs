module tests::rust::structured;

struct Point {
    x: i64,
    y: i64,
}

/// Add the coordinates of a point.
fn add(p: Point) -> i64 {
    p.x + p.y
}

fn main() -> i64 {
    let p = Point { x: 20, y: 22 };
    if add(p) == 42 { 0 } else { 1 }
}

module tests::rust::ffi;

extern "c" {
    fn labs(value: i64) -> i64;
    fn printf(format: ptr<i8>, ...) -> i32;
}

fn accepts_callback(callback: fnptr<c, [i64], i64>) -> i64 {
    0
}

fn exported(value: i64) -> i64 {
    labs(value)
}

export_c { exported as "coilrs_exported" };

fn main() -> i64 {
    if exported(-42) == 42 { 0 } else { 1 }
}

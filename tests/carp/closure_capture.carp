(defn add-one [x] (+ x 1))

(defn main []
  (let [captured 40
        add-captured (fn [x] (+ captured x))
        first-class add-one]
    (do
      (println* (~add-captured 2))
      (println* (~first-class 41)))))

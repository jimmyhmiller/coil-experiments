(module tests.borrow-checker.direct-owner-field)
(import "experiments.borrow-checker.borrow-checker" :use *)
(defstruct Item [(value i64)])
(defn main [] (-> i64)
  (let [item (box-new (Item :value 42))
        escaped (.value item)]
    (.value escaped)))

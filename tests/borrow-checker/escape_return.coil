(module tests.borrow-checker.escape-return)
(import "experiments.borrow-checker.borrow-checker" :use *)
(defstruct Item [(value i64)])
(defn escape [(item (ref (Box Item)))] (-> (ptr Item))
  (with-box-ref [view item] view))
(defn main [] (-> i64) 0)

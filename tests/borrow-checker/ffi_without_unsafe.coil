(module tests.borrow-checker.ffi-without-unsafe)
(import "experiments.borrow-checker.borrow-checker" :use *)
(extern puts :cc c [(ptr i8)] (-> i32))
(defn main [] (-> i64)
  (do (puts c"not allowed") 0))

(module tests.borrow-checker.raw-free)
(import "coil.primitive" :as primitive)
(import "experiments.borrow-checker.borrow-checker" :use *)
(defn main [] (-> i64)
  (let [pointer (primitive/alloc-heap i64)]
    (primitive/free (primitive/cast (ptr u8) pointer))
    0))

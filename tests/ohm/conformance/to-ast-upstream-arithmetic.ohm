// Ported from default/mapping cases in extras/test-toAST.js.
// @to-ast-upstream-arithmetic
// @accept 10 + 20
Arithmetic {
  Exp = AddExp
  AddExp
    = AddExp "+" PriExp  -- plus
    | AddExp "-" PriExp  -- minus
    | PriExp
  PriExp
    = "(" Exp ")"  -- paren
    | number
  number = digit+
}

// The empty branch commits before the inherited branch, so full matching "a"
// fails instead of reconsidering the super-splice after the end check.
// @accept-from letter 
// @reject-from letter a
SuperSpliceOrder {
  letter := "" | ...
}

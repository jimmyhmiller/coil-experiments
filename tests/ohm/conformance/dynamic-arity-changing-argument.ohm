// Ohm accepts a single sequence-valued dynamic argument even though it changes
// the binding arity of App's body. Its observable CST root is the leaked first
// terminal, not an App node, but its public Node interval spans the full match.
// @accept-from App<"a""b"> ab
DynamicArityChangingArgument {
  App<x> = x
}

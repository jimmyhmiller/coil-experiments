// Structural recipe coverage: parameters, inline cases, predicates, grouping,
// repetition, terminals, and generated parameter-forwarding applications.
// @accept a0
RecipeStructural {
  start = bar<"0", "1">
  bar<x, y>
    = ~"z" ("a" x)*  -- one
    | &"b" "b" y     -- two
}

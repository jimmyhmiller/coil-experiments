(module tests.ohm.interval-relative-coverage-error)

(import "experiments.ohm.runtime" :as ohm)

(defn main [] (-> i64)
  (let [source "0123456789"]
    (ohm/ohm-interval-relative-to
      (ohm/new-ohm-interval source 1 6)
      (ohm/new-ohm-interval source 2 8))
    1))

(module scheme-mal-stage1-bench-cli)
(import "coil.scheme" :use *)
(import "scheme-mal-stage1-bench" :use [mal-stage1-benchmark])

(defn main [] (-> i64)
  (display (mal-stage1-benchmark))
  (newline)
  (fixnum-value (mk-fixnum 0)))

(module scheme-mal-full-bench-cli)
(import "experiments.scheme.lang" :use *)
(import "scheme-mal-full-bench" :use *)
(import "scheme-mal-host" :as host)

(defn main [] (-> i64)
  (display (mal-full-benchmark))
  (newline)
  (host/exit-success))

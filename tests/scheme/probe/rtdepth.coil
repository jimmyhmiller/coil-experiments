(module rtdepth)
(import "coil.scheme" :use *)
(define (build n acc)
  (if (< n 1) acc (build (- n 1) (cons n acc))))
(define (len l acc)
  (if (null? l) acc (len (cdr l) (+ acc 1))))
(defn main [] (-> i64)
  (display (len (build 2000 '()) 0))
  (newline)
  (fixnum-value (mk-fixnum 0)))

(module tcprobe)
(import "coil.scheme" :use *)
;; Self tail recursion at a depth that would blow any real stack.
(define (count n acc)
  (if (< n 1) acc (count (- n 1) (+ acc 1))))
(defn main [] (-> i64)
  (display (count 50000000 0))
  (newline)
  (fixnum-value (mk-fixnum 0)))

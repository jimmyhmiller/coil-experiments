(module scheme-casefold-module-lib)
(import "coil.scheme" :use *)
(export SuM-To GaThEr-VaLuEs)

(DEFINE (SuM-To Limit)
  (LET LoOp ((N Limit) (Acc 0))
    (IF (= n 0) acc (LOOP (- N 1) (+ Acc N)))))

(DEFINE (GaThEr-VaLuEs First . Rest) (CONS First Rest))

(DEFINE (HiDdEn-PrOc X) X)

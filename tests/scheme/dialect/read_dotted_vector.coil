(module read-dotted-vector)
(import "experiments.scheme.lang" :use *)

(defn main [] (-> i64)
  (let ((p (open-input-file "tests/scheme/fixtures/dotted-vector.scm")))
    (read p))
  (fixnum-value (mk-fixnum 0)))

(module scheme-casefold-modules)
(import "coil.scheme" :use *)
(import "scheme-casefold-module-lib" :use *)

(defn main [] (-> i64)
  (WRITE (LIST (SUM-TO 10) (MAP SUM-TO '(3 4))
               (LET ((F GATHER-VALUES)) (F 1 2 3 4))))
  (NEWLINE)
  (fixnum-value (mk-fixnum 0)))

(module eval-duplicate-syntax-pattern)
(import "coil.scheme" :use *)

(defn main [] (-> i64)
  (eval '(define-syntax duplicate
           (syntax-rules () ((_ value value) value)))
        (scheme-report-environment 5))
  (fixnum-value (mk-fixnum 0)))

(module private-import-rejected)
(import "experiments.scheme.lang" :use *)
(import "scheme-casefold-module-lib" :use *)

(defn main [] (-> i64)
  (PROCEDURE? HIDDEN-PROC)
  (fixnum-value (mk-fixnum 0)))

(module read-incomplete-quote)
(import "experiments.scheme.lang" :use *)

(defn main [] (-> i64)
  (let ((p (open-input-file "tests/scheme/fixtures/incomplete-quote.scm")))
    (read p))
  (fixnum-value (mk-fixnum 0)))

(module read-invalid-plus-identifier)
(import "coil.scheme" :use *)

(defn main [] (-> i64)
  (let ((p (open-input-file "tests/scheme/fixtures/invalid-plus-identifier.scm")))
    (read p))
  (fixnum-value (mk-fixnum 0)))

(module quoted-lambda)
(import "experiments.scheme.lang" :use *)
(define quoted-procedure '(lambda (x) x))

(defn main [] (-> i64)
  (display '(lambda (x) x))
  (newline)
  (display quoted-procedure)
  (newline)
  (fixnum-value (mk-fixnum 0)))

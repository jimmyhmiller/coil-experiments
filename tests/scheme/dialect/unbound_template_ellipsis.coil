(module unbound-template-ellipsis)
(import "experiments.scheme.lang" :use *)

(define-syntax invalid-repeat
  (syntax-rules ()
    ((_ value) (list value introduced ...))))

(defn main [] (-> i64)
  (display (invalid-repeat 1))
  (fixnum-value (mk-fixnum 0)))

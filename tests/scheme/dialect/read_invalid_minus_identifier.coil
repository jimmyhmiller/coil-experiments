(module read-invalid-minus-identifier)
(import "coil.scheme" :use *)

(defn main [] (-> i64)
  (let ((p (open-input-file "tests/scheme/fixtures/invalid-minus-identifier.scm")))
    (read p))
  (fixnum-value (mk-fixnum 0)))

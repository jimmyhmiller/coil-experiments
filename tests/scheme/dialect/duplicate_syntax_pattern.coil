(module duplicate-syntax-pattern)
(import "experiments.scheme.lang" :use *)

(define-syntax duplicate
  (syntax-rules ()
    ((_ value value) value)))

(defn main [] (-> i64)
  (fixnum-value (mk-fixnum 0)))

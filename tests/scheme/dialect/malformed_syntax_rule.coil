(module malformed-syntax-rule)
(import "experiments.scheme.lang" :use *)

(define-syntax broken
  (syntax-rules ()
    ((_ value))))

(defn main [] (-> i64)
  (broken 1)
  (fixnum-value (mk-fixnum 0)))

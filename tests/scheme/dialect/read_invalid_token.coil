(module read-invalid-token)
(import "experiments.scheme.lang" :use *)

(defn main [] (-> i64)
  (let ((p (open-input-file "tests/scheme/fixtures/invalid-token.scm")))
    (read p))
  (fixnum-value (mk-fixnum 0)))

(module staged_scheme_bridge_test)
(import "staged_scheme_bridge" :use *)

;; (stx-bump answer 41) runs the compiled Scheme phase runtime at expansion
;; time: syntax object in, Scheme pair/fixnum compute, datum->syntax out -> 42.
(defn main [] (-> i64)
  (stx-bump answer 41))

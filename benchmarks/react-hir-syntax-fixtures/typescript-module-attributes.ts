import data from "data" with { type: "json", "resolution-mode": "import" };
import legacy from "legacy" assert { type: "json" };
export { value } from "data" with { type: "json" };
export * from "legacy" assert { type: "json" };
